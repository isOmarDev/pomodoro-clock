function handleReset(setWorkLength, setBreakLength, setMinutes, setSeconds, setIsOn, setIsWorkFinished) {
  setWorkLength(25)
  setBreakLength(5)
  setMinutes(25)
  setSeconds(0)
  setIsOn(false)
  setIsWorkFinished(false)
}

export default handleReset